#include "main.h"

/**
 * _abs - print the absolute value of an integer
 * @n: the int to validate
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
