#include "main.h"

/**
 * _isdigit - evaluate for a digit (0 through 9)
 * @c: int to be evaluated
 * Return: 1 when c is a digit, 0 if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
