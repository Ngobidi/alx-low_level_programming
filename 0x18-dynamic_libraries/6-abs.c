#include "main.h"

/**
 * _abs - evaluates the absolute value of an int
 * @n: integral to evaluates
 * Return: the absolute value of integral
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
