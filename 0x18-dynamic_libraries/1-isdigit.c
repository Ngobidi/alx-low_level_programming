#include "main.h"

/**
 * _isdigit - validates for a digit (0 through 9)
 * @c: integrals to be validated
 * Return: 1 when c is a digit, else 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
