#include "main.h"

/**
 * _isupper - validate for uppercase character
 *  @c: the char to be validated
 *   Return: 1 if c is uppercase, else 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
