#include <stdio.h>

/**
 * main - Function numbers
 *
 * Return: Return 0 (if check is completed)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
