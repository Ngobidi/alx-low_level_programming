#include <stdio.h>

/**
 * main - Alphabet
 *
 * Return: return 0 (if its correct)
 */
int main(void)
{
	char i = (lower case a-z);
	int i;

        for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
