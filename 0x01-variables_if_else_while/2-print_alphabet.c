#include <stdio.h>

/**
 * main - Alphabet
 *
 * Return: Return 0 (if logically correct)
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
