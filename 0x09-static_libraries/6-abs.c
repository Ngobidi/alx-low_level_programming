#include "main.h"

/**
 *  _abs - evaluate the absolute value of an int
 *  @n: the integer to validate
 *   Return: the absolute value of integal
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
