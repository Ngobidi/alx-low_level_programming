#include <unistd.h>

/**
 * _putchar - print the char c to stdout
 *  @c: The char to print
 *
 *  Return: alway 1. (when successful)
 *   else, return  -1
 */
int _putchar(char c)
{
	return (write(1, &c, 1));
}
