#include "main.h"

/**
 * main - tests function and write if integer is positive or negative
 *  Return: 0 (if logically correct)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
